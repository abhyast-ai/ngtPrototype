This file lists the Python packages required for your project. You can use it with pip install -r requirements.txt to install the necessary dependencies.
# The following are the packages needed by this project:
Python
ultranytics
numpy
